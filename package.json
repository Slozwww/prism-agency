{
  "name": "prism-agency-netlify",
  "version": "2.0.0",
  "description": "PRISM AGENCY - Site Netlify (statique + fonctions)",
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0",
    "nodemailer": "^6.9.7"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
